httpx>=0.27.0
rich>=13.7.0
textual>=0.52.0
pyperclip>=1.8.2
python-dotenv>=1.0.0
mss>=9.0.0
Pillow>=10.0.0
pyautogui>=0.9.54

# Browser Automation (for accessibility trees - 10x faster than vision)
# playwright>=1.50.0  # Uncomment if you want browser accessibility features
# pyppeteer>=0.0.25   # Fallback for playwright

# Cowork-style tools (optional - install as needed)
# openpyxl>=3.1.0       # Excel read/write
# xlsxwriter>=3.1.0     # Excel write
# PyPDF2>=3.0.0         # PDF text extraction
# pdfplumber>=0.10.0    # PDF with table extraction (better)
# PyMuPDF>=1.23.0       # Alternative PDF extraction

# Windows-specific (optional)
# pywin32>=306         # Better window management on Windows
# pygetwindow>=0.0.9   # Cross-platform window info (works on Windows)
